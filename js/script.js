
const getPokemons = async (url) => {
    const {data} = await axios.get(url);
    console.log("api" , data.game_indices);
    return data;
}
getPokemons ("https://pokeapi.co/api/v2/pokemon/ditto")





















const otrosPokemon = docume
.addEventListener('click', () => {
    printsVideos(containersCards, videoTube)
})
